#include <bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);
    int d;
    cin >> d;
    cout << fixed << setprecision(20) << M_PI * d * d / 4  << endl;
    return 0;
}
